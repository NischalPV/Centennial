import { FieldsErrorMatcher } from './fields-error-matcher';

describe('FieldsErrorMatcher', () => {
  it('should create an instance', () => {
    expect(new FieldsErrorMatcher()).toBeTruthy();
  });
});
